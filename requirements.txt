# law-monitor 의존성
# Phase 1부터는 공유 코어 라이브러리를 여기서 함께 설치합니다.

# ── 외부 패키지 ──────────────────────────────────────────────
requests>=2.31.0
urllib3>=2.0.0
google-genai
openpyxl>=3.1.2
gspread>=5.12.0
oauth2client>=4.1.3

# ── HRDK 공유 코어 (같은 계정의 private 레포에서 설치) ────────
# YOUR_GITHUB_ACCOUNT를 실제 깃허브 계정명으로 교체하세요.
hrdk-law-core @ git+https://x-access-token:${GITHUB_TOKEN}@github.com/gjtjdwns1008-dev/hrdk-law-core.git
